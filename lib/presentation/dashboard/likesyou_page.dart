import 'package:flutter/material.dart';

class LikesYouPage extends StatelessWidget {
  const LikesYouPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Your likes')));
  }
}
